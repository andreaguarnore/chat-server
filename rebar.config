{erl_opts, [debug_info]}.
{deps, []}.

{minimum_otp_vsn, "25"}.

{shell, [
  % {config, "config/sys.config"},
    {apps, [chat_server]}
]}.

{relx, [
    {release, {chat_server, "0.0.1"}, [chat_server]}
]}.
